# Inherit the Consumer Theme

Registry Items will inherit the Consumer Project's shadcn semantic colors, typography, radius, and dark-mode behavior rather than applying the Aeri UI website's brand theme. An item may define narrowly scoped motion variables with fallbacks, but it must not rewrite global theme tokens to reproduce its demo. The catalog will use an Aeri-branded presentation while verifying items against multiple neutral Consumer Themes.
