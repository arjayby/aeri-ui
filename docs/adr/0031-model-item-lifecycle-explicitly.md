# Model Registry Item lifecycle explicitly

Registry Items will move from Draft to Preview Item to Stable Item, and may later become a Deprecated Item while remaining documented and available. Drafts exist only in source and pull-request preview deployments; Preview APIs may change; Stable Items receive compatibility and migration guarantees; Deprecated Items identify a replacement and migration path. `New` is a temporary recency badge rather than a lifecycle state.
