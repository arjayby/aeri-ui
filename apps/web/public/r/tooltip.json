{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "tooltip",
  "title": "Tooltip",
  "author": "Aeri UI",
  "description": "A semantic Tooltip with a subtle focus and hover presence transition.",
  "dependencies": [
    "@base-ui/react"
  ],
  "files": [
    {
      "path": "registry/components/tooltip/tooltip.tsx",
      "content": "\"use client\";\n\nimport { Tooltip as TooltipPrimitive } from \"@base-ui/react/tooltip\";\nimport * as React from \"react\";\n\ntype TooltipContextValue = {\n\tcontentId: string;\n\tdescribedBy?: string;\n\topen: boolean;\n\tsetDescribedBy: React.Dispatch<React.SetStateAction<string | undefined>>;\n};\n\nconst TooltipContext = React.createContext<TooltipContextValue | undefined>(\n\tundefined,\n);\n\nfunction classNames(...values: Array<string | undefined>) {\n\treturn values.filter(Boolean).join(\" \");\n}\n\nfunction mergeClassName<State>(\n\tbaseClassName: string,\n\tclassName: string | ((state: State) => string | undefined) | undefined,\n) {\n\tif (typeof className === \"function\") {\n\t\treturn (state: State) => classNames(baseClassName, className(state));\n\t}\n\n\treturn classNames(baseClassName, className);\n}\n\ntype TooltipProps = TooltipPrimitive.Root.Props;\ntype TooltipProviderProps = TooltipPrimitive.Provider.Props;\ntype TooltipTriggerProps = TooltipPrimitive.Trigger.Props;\ntype TooltipContentProps = TooltipPrimitive.Popup.Props &\n\tPick<\n\t\tTooltipPrimitive.Positioner.Props,\n\t\t\"align\" | \"alignOffset\" | \"side\" | \"sideOffset\"\n\t>;\n\nfunction TooltipProvider({ delay = 500, ...props }: TooltipProviderProps) {\n\treturn <TooltipPrimitive.Provider delay={delay} {...props} />;\n}\n\nfunction Tooltip({\n\tchildren,\n\tdefaultOpen = false,\n\tonOpenChange,\n\topen: controlledOpen,\n\t...props\n}: TooltipProps) {\n\tconst contentId = React.useId();\n\tconst [describedBy, setDescribedBy] = React.useState<string>();\n\tconst [uncontrolledOpen, setUncontrolledOpen] = React.useState(defaultOpen);\n\tconst open = controlledOpen ?? uncontrolledOpen;\n\n\tfunction handleOpenChange(\n\t\tnextOpen: boolean,\n\t\teventDetails: TooltipPrimitive.Root.ChangeEventDetails,\n\t) {\n\t\tif (controlledOpen === undefined) {\n\t\t\tsetUncontrolledOpen(nextOpen);\n\t\t}\n\t\tonOpenChange?.(nextOpen, eventDetails);\n\t}\n\n\treturn (\n\t\t<TooltipContext.Provider\n\t\t\tvalue={{ contentId, describedBy, open, setDescribedBy }}\n\t\t>\n\t\t\t<TooltipPrimitive.Root\n\t\t\t\tdefaultOpen={defaultOpen}\n\t\t\t\tonOpenChange={handleOpenChange}\n\t\t\t\topen={controlledOpen}\n\t\t\t\t{...props}\n\t\t\t>\n\t\t\t\t{children}\n\t\t\t</TooltipPrimitive.Root>\n\t\t</TooltipContext.Provider>\n\t);\n}\n\nconst TooltipTrigger = React.forwardRef<HTMLElement, TooltipTriggerProps>(\n\tfunction TooltipTrigger(\n\t\t{ \"aria-describedby\": describedBy, className, ...props },\n\t\tref,\n\t) {\n\t\tconst context = React.useContext(TooltipContext);\n\t\tconst ariaDescribedBy = [describedBy, context?.open && context.describedBy]\n\t\t\t.filter(Boolean)\n\t\t\t.join(\" \");\n\n\t\treturn (\n\t\t\t<TooltipPrimitive.Trigger\n\t\t\t\taria-describedby={ariaDescribedBy || undefined}\n\t\t\t\tclassName={mergeClassName<TooltipPrimitive.Trigger.State>(\n\t\t\t\t\t\"outline-none focus-visible:outline-2 focus-visible:outline-ring focus-visible:outline-offset-2\",\n\t\t\t\t\tclassName,\n\t\t\t\t)}\n\t\t\t\tdata-slot=\"aeri-tooltip-trigger\"\n\t\t\t\tref={ref as React.Ref<HTMLButtonElement>}\n\t\t\t\t{...props}\n\t\t\t/>\n\t\t);\n\t},\n);\n\nconst TooltipContent = React.forwardRef<HTMLDivElement, TooltipContentProps>(\n\tfunction TooltipContent(\n\t\t{\n\t\t\talign = \"center\",\n\t\t\talignOffset = 0,\n\t\t\tchildren,\n\t\t\tclassName,\n\t\t\tid,\n\t\t\tside = \"top\",\n\t\t\tsideOffset = 8,\n\t\t\t...props\n\t\t},\n\t\tref,\n\t) {\n\t\tconst context = React.useContext(TooltipContext);\n\t\tconst contentId = id ?? context?.contentId;\n\n\t\tReact.useEffect(() => {\n\t\t\tif (!contentId) {\n\t\t\t\treturn;\n\t\t\t}\n\n\t\t\tcontext?.setDescribedBy(contentId);\n\t\t\treturn () =>\n\t\t\t\tcontext?.setDescribedBy((currentId) =>\n\t\t\t\t\tcurrentId === contentId ? undefined : currentId,\n\t\t\t\t);\n\t\t}, [contentId, context?.setDescribedBy]);\n\n\t\treturn (\n\t\t\t<TooltipPrimitive.Portal>\n\t\t\t\t<TooltipPrimitive.Positioner\n\t\t\t\t\talign={align}\n\t\t\t\t\talignOffset={alignOffset}\n\t\t\t\t\tclassName=\"z-50\"\n\t\t\t\t\tside={side}\n\t\t\t\t\tsideOffset={sideOffset}\n\t\t\t\t>\n\t\t\t\t\t<TooltipPrimitive.Popup\n\t\t\t\t\t\tclassName={mergeClassName<TooltipPrimitive.Popup.State>(\n\t\t\t\t\t\t\t\"z-50 max-w-xs origin-(--transform-origin) rounded-lg bg-foreground px-3 py-1.5 text-background text-xs shadow-md transition-[opacity,scale,translate] duration-150 ease-out data-[side=left]:data-[ending-style]:translate-x-1 data-[side=left]:data-[starting-style]:translate-x-1 data-[side=right]:data-[ending-style]:-translate-x-1 data-[side=right]:data-[starting-style]:-translate-x-1 data-[side=bottom]:data-[ending-style]:-translate-y-1 data-[side=bottom]:data-[starting-style]:-translate-y-1 data-[side=top]:data-[ending-style]:translate-y-1 data-[side=top]:data-[starting-style]:translate-y-1 data-[ending-style]:scale-95 data-[starting-style]:scale-95 data-[ending-style]:opacity-0 data-[starting-style]:opacity-0 motion-reduce:transition-none motion-reduce:data-[ending-style]:transform-none motion-reduce:data-[starting-style]:transform-none\",\n\t\t\t\t\t\t\tclassName,\n\t\t\t\t\t\t)}\n\t\t\t\t\t\tdata-slot=\"aeri-tooltip-content\"\n\t\t\t\t\t\tid={id ?? contentId}\n\t\t\t\t\t\trole=\"tooltip\"\n\t\t\t\t\t\tref={ref}\n\t\t\t\t\t\t{...props}\n\t\t\t\t\t>\n\t\t\t\t\t\t{children}\n\t\t\t\t\t\t<TooltipPrimitive.Arrow\n\t\t\t\t\t\t\tclassName=\"size-2.5 rotate-45 rounded-sm bg-foreground data-[side=bottom]:-top-1 data-[side=left]:-right-1 data-[side=top]:-bottom-1 data-[side=right]:-left-1\"\n\t\t\t\t\t\t\tdata-slot=\"aeri-tooltip-arrow\"\n\t\t\t\t\t\t/>\n\t\t\t\t\t</TooltipPrimitive.Popup>\n\t\t\t\t</TooltipPrimitive.Positioner>\n\t\t\t</TooltipPrimitive.Portal>\n\t\t);\n\t},\n);\n\nexport {\n\tTooltip,\n\tTooltipContent,\n\tTooltipProvider,\n\tTooltipTrigger,\n\ttype TooltipContentProps,\n\ttype TooltipProps,\n\ttype TooltipProviderProps,\n\ttype TooltipTriggerProps,\n};\n",
      "type": "registry:ui",
      "target": "components/aeri/tooltip.tsx"
    }
  ],
  "meta": {
    "catalog": {
      "boundary": "client",
      "implementation": "native",
      "publishedAt": "2026-07-28"
    },
    "lifecycle": "preview",
    "preview": {
      "label": "Tooltip preview",
      "interaction": "Focus or hover the control to reveal supporting context.",
      "fixture": "preview.tsx"
    },
    "example": "import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from \"@/components/aeri/tooltip\";\n\n<TooltipProvider>\n  <Tooltip>\n    <TooltipTrigger>More information</TooltipTrigger>\n    <TooltipContent>Additional context</TooltipContent>\n  </Tooltip>\n</TooltipProvider>",
    "changelog": [
      {
        "version": "0.1.0",
        "date": "2026-07-28",
        "summary": "Initial Preview Item release."
      }
    ],
    "sourceUrl": "https://github.com/arjayby/aeri-ui/tree/main/registry/components/tooltip/tooltip.tsx"
  },
  "docs": "## Purpose\n\nUse Tooltip to give concise, supplementary context for an already labelled control. Do not put essential information, interactive content, or a control's only accessible name in a Tooltip. It inherits the Consumer Theme rather than applying an Aeri UI theme.\n\n## Installation\n\nInstall with the Aeri UI registry command shown on this Item Page. Preview installations configure the Aeri UI registry URL in `components.json`.\n\n## API and anatomy\n\nCompose `TooltipProvider`, `Tooltip`, `TooltipTrigger`, and `TooltipContent`. Provider shares an opening delay among adjacent Tooltips and defaults to 500 milliseconds. Tooltip preserves Base UI controlled and uncontrolled `open` state, dismissal behavior, and trigger composition. Trigger accepts Base UI trigger props including `delay`, `closeDelay`, `closeOnClick`, `disabled`, and `render`. Content accepts Base UI popup props plus `side`, `sideOffset`, `align`, and `alignOffset`; it defaults to top, centered, with an 8 pixel offset. The API exposes no raw motion controls.\n\n## Dependencies and client boundary\n\n`@base-ui/react` is Tooltip's only runtime dependency. Tooltip reaches Base UI's client boundary, so using it adds that client component to a Consumer Project. The Catalog Live Preview adds further client state only to expose controls.\n\n## Accessibility\n\nThe Aeri wrapper supplies tooltip semantics and connects the trigger to its Content with `aria-describedby`. Base UI supplies keyboard focus behavior, Escape dismissal, and outside press dismissal. Keep the trigger keyboard reachable and give it its own concise accessible name. Tooltips are supplementary and are not a suitable way to expose required touch interaction or critical instructions.\n\n## Motion and performance\n\nContent uses one 150 millisecond opacity and transform presence transition, beginning only after the provider or trigger delay. Reduced motion removes that nonessential transition while preserving immediate open and dismissal state. There is no motion runtime, recurring timer, or animation frame loop after the transition settles.\n\n## Consumer Theme\n\nTooltip uses Consumer Theme semantic colors, typography, radius, and appearance. Override tokens in the Consumer Project and use `className` for local presentation changes. The source does not add a global stylesheet or rewrite global tokens.\n\n## Responsive, localization, and RTL\n\nTooltip constrains its content width and repositions around viewport collisions. Supply concise localized text as Content children. Base UI handles logical inline placement when `side` uses `inline-start` or `inline-end`; the default top placement is direction independent.\n\n## Compatibility\n\nTooltip supports Next.js 16 App Router Consumer Projects using React 19, Tailwind CSS 4, shadcn, and Base UI. It works in light and dark Consumer Theme appearances and remains functional offline after installation.",
  "categories": [
    "component",
    "overlay"
  ],
  "type": "registry:ui"
}